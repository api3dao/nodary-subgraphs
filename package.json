{
  "name": "subgraphs",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ bdrhn9/subgraphs",
    "create-local": "graph create --node http://localhost:8020/ bdrhn9/subgraphs",
    "remove-local": "graph remove --node http://localhost:8020/ bdrhn9/subgraphs",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bdrhn9/subgraphs",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.53.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
